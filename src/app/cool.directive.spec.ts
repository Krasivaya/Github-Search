import { CoolDirective } from './cool.directive';

describe('CoolDirective', () => {
  it('should create an instance', () => {
    const directive = new CoolDirective();
    expect(directive).toBeTruthy();
  });
});
